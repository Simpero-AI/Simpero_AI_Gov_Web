# FROZEN snapshot — do not edit

Machine-generated `.d.ts` snapshot of the Express tRPC router from
`simpero_GOV_AI @ 4cdfe5ce1c382febf777e5289ee2e209d0c4479f`
(`tsc --declaration --emitDeclarationOnly` on `server/routers.ts`).

It exists only so screens stay type-checked against the old backend during the
tRPC → OpenAPI migration (playbook step FE-4). Deleted in FE-7 together with
`src/lib/trpc.ts` and `ClerkTrpcProvider`.

To refresh (only if a dual-applied bugfix changes the old API surface):
re-run the declaration emit in the monorepo and replace this directory wholesale.
